name: Run Black linter

on:
  pull_request:
    branches:
    - master

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Check Python version
      run: python --version
    - name: Install Black
      run: pip install black
    - name: Run Black checker
      run: black --check portal/
